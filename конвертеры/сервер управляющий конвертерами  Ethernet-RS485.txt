Нативное Node JS серверное приложение  для управления работой конвертеров Z-397 web (RS485 в Eternet). 

Решаемые задачи:
1) поиск веб конвертеров Z-397 web (RS485 в Eternet) и добавление их в список доступных
Используется широковещательный запрос UDP, в ответ конвертеры отправляют информацию о себе. Подробнее в прикрепленном файле.
ответ конвертера
{
  from: '192.168.1.89',
  'Z397-WEB-SW': '4.0.13',
  number: '48424',
  L1_Port: '1000',
  L2_Port: '1001',
  L1_Conn: '0.0.0.0',
  L2_Conn: '0.0.0.0',
  Lock: '0'
}
2)Добавление в список доступных конвертеров работающих в режиме клиент и  настроенных на работу с серавером (присылают запросы)
3) По команде админа приложение подключается/отключается к конвертеру. Запоминает сделаный выбор и при необходимости восстанавливает подключение.
4) Переключение скорости передачи конвертера на выбраннуюю. Выбор так же запоминается и при восстановлении снова восстанавливается.  
После этого конвертер поддерживает протокол NVT, что означает маскирование FF.
4) Для управления приложение спользуются команды API 


Список серверов
- Сервер UDP для поиска конвертеров, отправляет широковещательные запросы
- Сервер TCP socket для подключения конвертеров как клиентов
- Клиент TCP socket для подключения к конвертерам серверам
- Сервер HTTP для показа HTML страниц управляющих приложением через POST запросы и по Websocket
- WEBSOCKET для двухсторонней связи с HTML страницами


В консоли приложения выводиятся:
- локальный адрес и порт,  сетевой адрес  и порт для открытия главной страницы, если у компьютера несколько сетей, значит выводится  несколько адресов.
- локальный адрес и порт,  сетевой адрес  и порт для подключения конвертеров в режиме клиент.
- лог событий и ошибок (включается и выключается  командой)

Структура HTTP сервера 
index.html
	/pages - для HTML страниц
	/stands - для хранения JSON файлов
	/cmd - для хранения JSON файлов
	/lists - для хранения JSON файлов

Главная страница index.html предназначена для показа ссылок и описаний страниц находящихся в папке /pages
Ссылки и описания создаются автоматитечки на основе названия файла и информации в поле Title каждой странице, обновление выполняется по нажатию кнопки на странице index.html

Протокол связи с приложением:
JSON строка объекта
api: api,
cmd: cmd,
data: {
name:value,
....
}
Каждая страница имеет свое название для api, соответстующее ее названию (для admin.html будет admin)
Для каждого api создается свой модуль с функцими, отдельный файл.
При получении команды приложение проверяет соответстующий api объект на наличие указанной cmd = функции,
если ее нет то сообщает - функция не найдена
если есть то вызывает ее с параметрами из data.


В папке /pages создается страница admin.html,  Title =  Управление конвертерами
(остальные страницы будут созданы позже, проект создается частями)
Страница admin.html состоит издвух вертикольных блоков 
1) Слнева окно для  списка конвертеров,
сверху кнопка - включить/выключить поиковые UDP запросы.
ниже болк для пказа списка конвертеров, показывается IP адрес
Возможностями CSS выделяем 
- подключен, не подключен (но на связи = активен),
-  не активен конвертер, 
- режим работы клиент или сервер, 
- удален (автоматическиподключение не восстанавливать).
- подключен к другому приложению (серверу) (подключиться не получится)
2) справа окно для вывода запрашиваемой информации^
При клике по конвертеру (адресу), в левом окне,  открывается окно с информацией о конвертерев верхнней части этого окна кнопки:
-  подключить/отключить, 
- выпадающий список выбора скорости, команда смены скорости отправляется сразу. Варианты (230400 - по умолчанию,  115200, 38400)
- информация. Отправка запроса информации о конвертере = 0x69  0x0D.
- удалить конвертер (автоматически не восстанавливать соединение), если нажать  подключить то удаление отменяется.

В папке /pages создается страница logs.html,  Title =  Лог обмена данными с конвертером
страница состоит из двух горизонтальных окон
Верхнее окно с кнопками управления:
- включить/выключить вывод лога, 
- показывать/не показывать байты запроса и ответа
Нижнее окно для показа лога






Примеры реализации поиска конвертера
//UDP server  поиск конвертеров -------------->>-----------------------
const port_udp = 9000;
const server_udp = dgram.createSocket("udp4");
server_udp.bind(function() {
    server_udp.setBroadcast(true);
    setInterval(broadcastNew, 3000);
    //setTimeout(broadcastNew, 3000);
});
server_udp.on('message', function (message, rinfo) {
	let msg={};
	msg.from=rinfo.address;
	let str=String(message);
	arr=str.split(' ');
	for(let i in arr){
		let k=arr[i].split(':');
		if(k.length>1){
			msg[k[0]]=k[1];
		}else{
			let ind=k[0].indexOf('SN');
			if(k[0].includes('SN')){
				msg.number=k[0].slice(ind+2); 
			}
		}
	}
	if((msg.L1_Port==port_client)||(msg.L2_Port==port_client)){
		//console.log(msg);
		if((msg.L1_Conn=='0.0.0.0')&&(msg.L2_Conn=='0.0.0.0')){
			//console.log(msg);
			new_client(msg.from);
		}
	}
});
function broadcastNew() {
    var message = Buffer.from("SEEK Z397IP");
    server_udp.send(message, 0, message.length, port_udp, broadcast_adr, function() {
        //console.log("Sent '" + message + "'");
    });
}


// установить скорость линии 230400:
	obj.socket.write(Buffer.from([0xFF, 0xFA, 0x2C, 0x01, 0x00, 0x03, 0x84, 0x00, 0xFF, 0xF0]));
	obj.socket.on('data', function(data) {
		obj.data=data;
		func_api.answer(obj);	
	});


Если, что то не описано,  перед созданием сервера  задай уточняющий вопрос.





Для взаимодействия каналов связи с конвертером для каждого конвертера создается очередь команд (объект очереди).
По этому, создаем класс создающий такие объекты.
Конвертер  работает по принципу Запрос-Ответ. 
Если ответ не получен в течении заданного времени,  считается, что ответ не получен.
Задача объекта организовать очередь выполнения команд.
Очередь состоит из двух списков основного и оперативного. Списки это Set().
Сначала выполняются команды оперативного списка, потом команда основного списка,  если оперативный список пуст выполняется следующая команда основного списка.
Когда оба списка пусты в канал ни чего не отправляется.
Для добавления команд в очередь объект имеет метод add. При его вызове указывается в какой из списков нужно добавить команду.
Добавлять команды в очередь можно в любой момент. 
Если  очередь пуста, то как только в очередь добавляется  команда, очередь, начинает обрабатываться.
Необходимо предусмотреть настройку паузу после получения ответа до отправки следующего запроса.
В класс передается указатель на функцию отправки сообщений.
В объект, метод add передаются:
- тип списка (основной или оперативный)
- ссылка на экземпляр команды (функция генератор =  function *)
- calback для отправки ответа каналу который  добавил команду в очередь
- время ожидания ответа, после которого считается, что ответа нет
В объект, метод listener передаются сообщения (ответы) коннвертера.


1) Порт для TCP подключения: 1000 (основной)
2) Состояние сохраняется в JSON файлах в папке /settings
3) Обработка множественных подключений:
Может ли один конвертер одновременно подключаться к нескольким клиентам?
- нет
Нужно ли проверять, что конвертер не занят другим приложением?
- да,  это видно в ответе на поисковый запрос, конвертер в режиме клиент сам отправляет запросы -значит свободен.
4)Структура API модулей:
- Файлы модулей для API будут называться admin.js, logs.js
- Они должны экспортировать объект с функциями-обработчиками
5) WebSocket сообщения
Какие события должны отправляться через WebSocket
- команды и ответы API
- ошибки
В каком формате?
- ошибки в виде текста, для показа на странице logs
6) Страница admin.html:
Нужно ли показывать серийный номер и другую информацию из UDP ответа?
- нужно
Как определять, что конвертер "подключен к другому приложению"?
- в ответе на посковые запросы конвертер сам сообщает об этом
  L1_Conn: '0.0.0.0',
  L2_Conn: '0.0.0.0',
7) Лог обмена:
Лог должен показывать в зависимости от нажатых кнопок на странице logs
- лог не показывается
- показываются названия команд и успешно/нет
- байты отправленного запроса и байты ответа
8) Обработка ошибок:
Как обрабатывать ситуацию, когда конвертер перестал отвечать после подключения?
- сбрасываем соединение через 5 сек. Пытаемся восстановить соединение.








